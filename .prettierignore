# Add files here to ignore them from prettier formatting
/dist
/coverage
/.nx/cache
/.nx/workspace-data
.nx/self-healing
# Docker MCP Catalog entry: formatted with Prettier defaults (double quotes) to match docker/mcp-registry CI
tools/directories/docker-mcp-registry/
